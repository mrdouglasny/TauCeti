-- This is the AI-owned root of the Centauri mathematics library.
--
-- Everything reachable from here must be sorry-free, and must not import
-- `CentauriRoadmap` or `CentauriReview` (both enforced in CI). As the library
-- grows, import the submodules of `Centauri/` here.
import Centauri.Basic
