import Mathlib

/-!
# Jacobian challenge (AG version) — target signatures

The narrative roadmap, the schematic statements, and the acceptance criteria are in
`README.md`. The compiled targets depend on scheme-level Picard / abelian-variety
API that is still developing upstream, so for now the statements live as sketches in
`README.md`.

Promote them into `sorry`-stated targets here (human-owned roadmap territory, so
`sorry` is allowed) once the pinned Mathlib commit supports them — starting with
`noncomputable def Jacobian` and the universal property of the Abel–Jacobi map.
-/

namespace CentauriRoadmap.JacobianChallenge

-- (no compiled targets yet; see README.md)

end CentauriRoadmap.JacobianChallenge
